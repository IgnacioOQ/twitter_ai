# Workspace Rules

## Git Rules
- NEVER commit and push to remote repositories (e.g., GitHub) without the user's explicit authorization. You may commit locally, but always ask for permission before pushing.
